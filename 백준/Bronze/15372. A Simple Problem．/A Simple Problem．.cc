#include <iostream>
#include <vector>
#include <algorithm>
#include <cstring>
#include <cmath>

using namespace std;

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    long long t, n, m, k, l;
    cin >> t;
    while (t--)
    {
        int sum;
        cin >> n;
        long long a = pow(n,2);
        cout << a << '\n';
    }
    return 0;
}